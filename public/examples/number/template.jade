template
  style.
    :host {display: block; padding: 20px; background: #900;  margin-bottom: 10px;}
  div
    label ${label}
    input(type="number" value="${num}")
  script.
    $(body)
    .on("change keyup", "input", function (){
      //- debugger
      data.num = new Number($(this).val()).valueOf()
    })
